services:
  front:
    image: 404welldone/frontend:latest
    container_name: frontend
    ports:
      - '127.0.0.1:3000:3000'
    restart: always
    networks:
      - welldone_net
networks:
  welldone_net:
    driver: bridge
